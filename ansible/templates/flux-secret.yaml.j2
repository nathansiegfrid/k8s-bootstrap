# Deployed by Ansible.
# Do not edit manually, changes will be overwritten.

apiVersion: v1
kind: Secret
metadata:
  name: flux-system
  namespace: flux-system
type: Opaque
data:
  identity: "{{ flux_deploy_key | b64encode }}"
  identity.pub: "{{ flux_deploy_key_pub | b64encode }}"
  known_hosts: "{{ flux_known_hosts | b64encode }}"
